Get-CimInstance Win32_Process |
  Where-Object { $_.Name -eq 'node.exe' -and $_.CommandLine -match '@sassoftware/mcp-serverjs' } |
  Format-Table ProcessId,CommandLine -AutoSize

 Stop-Process -Id <PID> -Forcenpm run debug
